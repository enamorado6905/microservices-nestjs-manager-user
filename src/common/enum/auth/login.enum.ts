/**
 * Enumeration of login platforms or methods.
 * This enum distinguishes between different methods or platforms from which a login can be initiated.
 */
export enum LoginEnum {
  Web = 'web', // Represents a login initiated from a web platform.
  Apk = 'apk', // Represents a login initiated from an APK (Android Package Kit) platform.
}
